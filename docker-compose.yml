version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: garage_backend
    environment:
      DATABASE_URL: postgresql://garage_user:GaragePass456!@localhost:5432/garage_go?sslmode=disable
      JWT_SECRET: 7f82670b53bd936d32f73af55c021a82
      JWT_REFRESH_SECRET: 673d0b0d0fc538ae34a3d177f682d2ac
      PORT: 8080
      CORS_ORIGIN: http://178.105.209.59
      CUSTOMER_CORS_ORIGIN: http://178.105.209.59
      MECHANIC_CORS_ORIGIN: http://178.105.209.59
      SKIP_SCHEMA_EXECUTION: "true"
      DEFAULT_ADMIN_PASSWORD: admin123
      DEFAULT_RECEPTIONIST_PASSWORD: receptionist123
    network_mode: host
    restart: unless-stopped

  nginx:
    image: nginx:alpine
    container_name: garage_nginx
    volumes:
      - ./admin_frontend/build/web:/usr/share/nginx/html/admin
      - ./customer-frontend:/usr/share/nginx/html/customer
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    network_mode: host
    restart: unless-stopped